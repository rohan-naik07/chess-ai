import React from "react";

const styles = {
    title : {

        
    }
}


const Auth = (props)=>{
    const [inputData,setInputData] = React.useState({
        name : '',
        password : ''
    });

    return (
        <div>

        </div>
    );
}

export default Auth;